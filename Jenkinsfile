pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo 'building the project'
                git branch:'master', url:'https://github.com/Akashrajkarkera/Pipeline-.git'
                sh 'ls'
            }
        } 
    }
}
